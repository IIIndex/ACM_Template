二分图最大匹配：
	在读入的时候，将左边点和右边点分开处理；例如<1,1>，我们把第一个1放在左边的点集中，第二个1放在右边的点集中，（可以看作是<1,1+n>这样的边来处理），建立一条权值为1的边（代表一次匹配）。
	随后，我们建立一个原点，一个汇点，原点和所有的左边点建立一条权值为1的边（代表一个点只能用一次），汇点和所有的右边点建立一条权值为1的边（同理），然后跑一遍网络流即为最大匹配数。